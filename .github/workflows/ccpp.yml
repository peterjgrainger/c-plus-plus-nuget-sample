name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: dir
      run: dir
    - name: ls
      run: ls
    - name: compile
      run: |
        run.bat
