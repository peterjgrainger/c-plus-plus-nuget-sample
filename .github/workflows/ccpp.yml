name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: dir
      run: dir
    - name: ls
      run: ls
    - name: compile
      run: |
        "C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\Common7\IDE\devenv.com" "d:\a\c-plus-plus-nuget-sample\c-plus-plus-nuget-sample/Project1.sln" /build Release /project test
