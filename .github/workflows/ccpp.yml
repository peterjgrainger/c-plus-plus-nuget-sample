name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: D:\a\c-plus-plus-nuget-sample\c-plus-plus-nuget-sample\build.bat
